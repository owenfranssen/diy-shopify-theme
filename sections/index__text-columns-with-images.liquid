{% comment %}
** Text columns with images dynamic section

{% endcomment %}

<section>
  {% if section.settings.title != blank %}
    <h2 class="section__title">{{ section.settings.title }}</h2>
  {% endif %}
</section>

{% schema %}
  {
    "name": "Text columns iwth images",
    "class": "section__text-columns-with-images",
    "settings": [
      {
        "type": "text",
        "id": "title",
        "label": "Heading",
        "default": "Title"
      }
    ],
    "blocks": [
    ],
    "presets": [
      {
        "name": "Text columns with images",
        "category": "Text",
        "settings": {}
      }
    ]
  }
{% endschema %}