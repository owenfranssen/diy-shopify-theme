{% comment %}
** Featured promotion dynamic section

Same as featured collections ?
{% endcomment %}

<section>
  {% if section.settings.title != blank %}
    <h2 class="section__title">{{ section.settings.title }}</h2>
  {% endif %}
</section>

{% schema %}
  {
    "name": "Featred promotion",
    "class": "section__featured-promotion",
    "settings": [
      {
        "type": "text",
        "id": "title",
        "label": "Heading",
        "default": "Title"
      }
    ],
    "blocks": [
    ],
    "presets": [
      {
        "name": "Featured promotion",
        "category": "Promotion",
        "settings": {}
      }
    ]
  }
{% endschema %}