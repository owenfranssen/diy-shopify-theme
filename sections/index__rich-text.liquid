{% comment %}
** Rich text dynamic section

{% endcomment %}

<section>
  {% if section.settings.title != blank %}
    <h2 class="section__title">{{ section.settings.title }}</h2>
  {% endif %}
</section>

{% schema %}
  {
    "name": "Rich text",
    "class": "section__rich-text",
    "settings": [
      {
        "type": "text",
        "id": "title",
        "label": "Heading",
        "default": "Title"
      }
    ],
    "blocks": [
    ],
    "presets": [
      {
        "name": "Rich text",
        "category": "Text",
        "settings": {}
      }
    ]
  }
{% endschema %}