{% comment %}
** Blog posts dynamic section

{% endcomment %}

<section>
  {% if section.settings.title != blank %}
    <h2 class="section__title">{{ section.settings.title }}</h2>
  {% endif %}
</section>

{% schema %}
  {
    "name": "Blog posts",
    "class": "section__blog-posts",
    "settings": [
      {
        "type": "text",
        "id": "title",
        "label": "Heading",
        "default": "Title"
      }
    ],
    "presets": [
      {
        "name": "Blog posts",
        "category": "Marketing",
        "settings": {
          "title": "Blog Posts"
        }
      }
    ]
  }
{% endschema %}
