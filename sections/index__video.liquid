{% comment %}
** Video dynamic section

{% endcomment %}

<section>
  {% if section.settings.title != blank %}
    <h2 class="section__title">{{ section.settings.title }}</h2>
  {% endif %}
</section>

{% schema %}
  {
    "name": "Video",
    "class": "section__video",
    "settings": [
      {
        "type": "text",
        "id": "title",
        "label": "Heading",
        "default": "Title"
      }
    ],
    "blocks": [
    ],
    "presets": [
      {
        "name": "Video",
        "category": "Media",
        "settings": {}
      }
    ]
  }
{% endschema %}