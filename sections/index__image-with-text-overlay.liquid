{% comment %}
** Image with text overlay dynamic section

{% endcomment %}

<section>
  {% if section.settings.title != blank %}
    <h2 class="section__title">{{ section.settings.title }}</h2>
  {% endif %}
</section>

{% schema %}
  {
    "name": "Image with text overlay",
    "class": "section__image-with-text",
    "settings": [
      {
        "type": "text",
        "id": "title",
        "label": "Heading",
        "default": "Title"
      }
    ],
    "blocks": [
    ],
    "presets": [
      {
        "name": "Image with text overlay",
        "category": "Image",
        "settings": {}
      }
    ]
  }
{% endschema %}